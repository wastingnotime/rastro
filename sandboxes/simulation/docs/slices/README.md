# Simulation slice documents

Each slice document records the selected behavior, scenario, deterministic
tests, invariants, and explicit out-of-scope boundaries. Candidate slices may
change as the shared simulation disproves the model hypothesis.
